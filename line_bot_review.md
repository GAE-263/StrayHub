# LINE bot 操作流程檢視與修改計畫

日期：2026-09-06

## 目標與範圍

改善台灣收容所志工與領養人在 LINE 內的操作順暢度，優先處理流程互相干擾、資料不易修改及入口提示不完整的問題。本文件為待實作計畫，尚未修改 LINE bot 功能。

保留志工既有六項觀察、照片、補充與故事內容，以及領養問卷與 AI 分析邏輯。這次不擴充報到系統，也不調整後台回報 AI 整理功能。

## 檢視基礎

- 已檢查 webhook 路由、志工與領養對話狀態、訊息卡片及選單處理。
- 相關 104 項測試通過，涵蓋狀態轉移、草稿續填、領養送出、重複事件與跨收容所拒絕存取。
- 尚未以實機 LINE 驗證；既有測試通過不代表混合身分操作及以下卡點已受測試涵蓋。

## 一、優先修正跨流程訊息分派

### 問題與情境

同一人可能既是志工，也正在考慮領養。領養草稿未完成時，即使切回志工入口，webhook 仍優先把文字與圖片交給領養流程。志工因此可能無法正常補充心得或上傳回報照片。

### 修改方式

1. 先檢查現有草稿、身分與對話狀態儲存方式，沿用既有模式記錄使用者目前操作的流程；如需新增持久化欄位，再依既有 migration 流程設計。
2. 使用者明確進入「散步回報」「領養媒合」或「毛孩日記」時，切換目前流程。文字與圖片依目前流程分派，不以哪一種草稿存在作為固定優先順序。
3. 保留另一流程的未完成草稿，切回時續填；切換不等於取消或送出。
4. 「返回主選單」暫停目前輸入流程；此時收到文字或圖片，提示先選擇服務入口，避免誤寫入舊草稿。
5. 舊卡片的按鈕須檢查所屬流程與目前步驟。流程不符時提示先切回，不直接修改另一份草稿。
6. 兼容沒有目前流程紀錄的既有使用者：只有一份有效草稿時可恢復；有多份時提供入口選擇，不猜測輸入用途。

### 驗收情境

- 領養問卷填到一半 → 開始志工回報 → 完成六題、照片與補充 → 成功送出；原領養答案保留。
- 志工回報填到一半 → 進入領養流程 → 姓名與電話不會寫入志工心得。
- 返回主選單後輸入文字，不會修改任何草稿。
- 切換後點擊舊流程卡片、草稿過期或重複收到 webhook，均有明確回應且不誤送出。
- 毛孩日記與上述兩條流程互切時，照片與文字歸屬正確。

主要位置：`services/api/app/api/line_webhook.py`、相關 draft service／repository、`line_rich_menu_routing.py`。

## 二、改善領養聯絡資料修改

### 問題與情境

領養意願摘要的「修改」目前只返回電話欄位；姓名與方便聯繫時間畫面沒有明確的上一步。民眾最後確認時才發現姓名填錯，難以回頭修正。

### 修改方式

1. 摘要提供「修改姓名」「修改聯絡時間」「修改電話」三個明確操作。
2. 選擇欄位後顯示目前內容與輸入方式；驗證通過即返回摘要，保留其他答案。
3. 提供「返回摘要」以放棄本次修改；使用獨立編輯狀態或既有等效機制，避免一般 `back()` 清除後續答案。
4. 電話沿用台灣手機格式驗證，錯誤訊息包含可理解的範例。
5. 修改聯絡資料不重新執行 AI 分析，也不建立新的領養意願。

### 驗收情境

- 從摘要分別修改姓名、聯絡時間與電話，其他資料均保留。
- 輸入不合法電話後可重試，舊有效資料不因失敗而遺失。
- 放棄修改可回摘要；重複點擊送出只建立一筆意願。
- 編輯途中切換入口後再回來，可以恢復正確畫面。

主要位置：`line_webhook.py`、`domain/line_adoption_state.py`、`application/line_adoption_conversation.py`。

## 三、補齊「心有所屬」動物瀏覽與搜尋

### 問題與情境

目前只顯示前 12 隻動物，沒有下一頁。雖可輸入名稱或收容編號搜尋，畫面沒有明確說明，民眾可能找不到在現場看過的動物。

### 修改方式

1. 動物清單加上「可輸入名字或收容編號搜尋」說明。
2. 提供上一頁／下一頁及目前頁次，每頁維持 LINE 卡片可接受的數量。
3. 使用穩定排序與收容所範圍查詢；檢查現有 repository 是否可沿用分頁能力。
4. 搜尋多筆時顯示候選清單；零筆時提示檢查名稱／編號，並提供返回清單操作。
5. 最終選取時再次確認動物仍可領養；若狀態已變更，提示重新選擇。

### 驗收情境

- 超過 12 隻動物的收容所，可找到並選取第 13 隻以後的動物。
- 零筆、單筆、多筆搜尋都有可繼續操作的畫面。
- 翻頁及搜尋只顯示所選收容所的可領養動物。
- 清單顯示後動物改為不可領養，選取時被拒絕並提供返回入口。

主要位置：`line_webhook.py`、`application/line_adoption_flex.py`、`persistence/repositories/animal_repository.py`。

## 四、移除尚未開放的志工報到引導

### 問題與情境

切回志工選單時提示使用「志工報到」，但該功能目前僅回覆開發中。新志工可能誤以為點過就完成報到。

### 修改方式

1. 將切回志工選單的提示改為已可使用的「散步回報」。
2. 檢查實際選單定義與圖片；若含報到入口，移除或明確標示尚未開放。
3. 舊選單仍可能送來 `volunteer_checkin`，保留相容回覆：「線上報到尚未開放，請向現場工作人員確認報到方式。」不得回覆報到成功。
4. 程式、選單定義與圖片一起更新；實際 LINE 選單同步另列部署步驟，不能只更新程式即視為完成。

### 驗收情境

- 新選單與切換提示不會引導使用者進入未完成的功能。
- 舊入口仍有清楚回應，不造成已完成報到的誤解。
- 散步回報及返回主選單功能維持正常。

主要位置：`line_webhook.py`、`application/line_menu_actions.py`、`scripts/sync_line_role_menus.py` 及實際選單資產。

## 實作順序與共通要求

1. 優先完成跨流程分派與回歸測試，作為後續編輯與續填的基礎。
2. 完成領養聯絡資料編輯。
3. 完成動物分頁與搜尋提示。
4. 整理志工報到入口、選單圖片與提示。
5. 執行整合測試，再以測試 LINE 帳號進行實機驗收。

每項可獨立驗收。實作前先檢查 router、schema、service、repository、授權與測試；若影響 API 合約，同步檢查前端使用處。

- 所有草稿與操作皆由後端核對使用者歸屬及收容所權限，不信任 postback 傳入的身分或收容所。
- 公開領養資料沿用公開可領養範圍，不能因新增搜尋而暴露非公開資料。
- 保留 LINE 簽章驗證、快速 webhook 回覆及重複事件處理。
- 文字使用台灣繁體中文；使用者畫面不顯示 JSON、狀態代碼或技術錯誤。
- 本計畫不包含自行發布選單、傳送通知、提交 commit 或推送遠端。

## 驗證與完成條件

- 補上混合身分／多草稿訊息分派測試，不只測試各自獨立的正常流程。
- 補上摘要單欄編輯、分頁搜尋、舊卡片與舊選單相容測試。
- 重跑既有志工、領養、草稿續填、冪等與收容所隔離相關測試。
- 若調整資料結構，驗證既有草稿相容性及 migration；若改動前端，執行相應型別與建置檢查。
- 以測試 LINE 帳號實際走完：志工回報、兩條領養路徑、聯絡資料修改、跨入口續填與照片上傳。
- 完成時記錄通過項目、實機結果及仍需同步的選單資產，檢查 `git status` 與 `git diff`，確認沒有無關變更。

## 實作紀錄

### 功能一：目前操作流程

- 已新增 `0048_line_current_flow`，保存每個 LINE 綁定目前使用的流程；不作為收容所授權依據。
- 明確入口切換、主選單暫停、舊卡片拒絕及既有單一草稿恢復已實作；多草稿需選擇入口。
- 保留其他草稿，並修正領養流程收到照片時提示卡片缺少必要參數的錯誤。
- 驗證包含同一使用者的領養草稿／志工補充、主選單暫停、舊卡片及既有草稿判斷。
- 本機專用測試資料庫已套用 migration；實際服務資料庫與實機 LINE 驗收仍需部署時處理。

### 功能二：領養聯絡資料單欄修改

- 已提供修改姓名、聯絡時間、電話及返回摘要操作；每次僅更新指定欄位，其他問卷資料保留。
- 使用獨立編輯狀態，重新進入領養流程仍可續填；無效電話保留舊值，不重跑 AI。
- 已驗證單欄修改、取消、錯誤重試、續填及修改後送出意願。

### 功能三：動物分頁與搜尋

- 心有所屬改為每頁 12 隻，提供頁次、上一頁、下一頁及返回領養方式。
- 支援 20 字以內名稱／編號搜尋，多筆顯示可選清單，零筆可返回全部清單；搜尋字元按字面匹配。
- 查詢在資料庫依所選收容所、有效及可領養狀態過濾並穩定排序；確認動物時再次驗證狀態。
- 24 項相關測試通過，包含超過 12 隻、搜尋分頁、零筆／單筆、其他收容所隔離及選取後狀態改變。
