# Copy to an ignored terraform.tfvars only after confirming the active account and project.
project_id = "canvas-primacy-502703-k1"
region     = "us-central1"
zone       = "us-central1-c"

# Optional reviewed overrides:
# environment       = "production"
# instance_name     = "strayhub-gce"
# machine_type      = "e2-medium"
# boot_disk_size_gb = 30
# web_upstream_port = 3000
# api_upstream_port = 8080
